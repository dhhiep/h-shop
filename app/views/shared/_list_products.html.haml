#product-items
  - Array.new(60).in_groups_of(4).each do |group|
    .row.line-product-item
      - group.each do |item|
        .col-xs-12.col-sm-6.col-md-3.col-lg-3
          = render "shared/product"
